apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: http
      name: http
    - port: 8080
      targetPort: info
      name: info
  selector:
    app: {{ .Release.Name }}
