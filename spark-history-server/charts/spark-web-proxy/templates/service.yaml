apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-web-proxy
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: http
      name: http
  selector:
    app: {{ .Release.Name }}-web-proxy
